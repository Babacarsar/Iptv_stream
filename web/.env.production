# GitHub Pages — repo Iptv_stream
VITE_BASE_PATH=/Iptv_stream/

# Proxy CORS (obligatoire pour chaînes SN en http://)
# 1. cd worker && npm install && npx wrangler login && npx wrangler deploy
# 2. Décommente et remplace par ton URL workers.dev :
# VITE_STREAM_PROXY=https://babacar-stream-proxy.TON-SUBDOMAIN.workers.dev
